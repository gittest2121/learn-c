/* Print a Horizontal Histogram of lengths of words
 * from its input.
 * Using Arrays */
#include <stdio.h>
int main(void) {
	int i, j, values;
	printf("Number of Values: ");
	scanf("%d", &values);
	printf("%d iii", values);
}
